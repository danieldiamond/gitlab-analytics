name: 'gitlab_dw'
version: '1.0'

profile: 'gitlab'

source-paths: ["models"]

models:
  sfdc:
      base:
          enabled: true
          materialized: view
      transformed:
          enabled: true
          materialized: table
