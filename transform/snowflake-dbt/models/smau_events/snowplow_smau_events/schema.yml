version: 2

models:
  - name: create_snowplow_smau_events
    description: '{{ doc("create_snowplow_smau_events") }}'
    columns:
      - name: user_snowplow_domain_id
        description: UUID which is generated by the Javascript tracker and stored in a (first party) cookie
        tests:
          - not_null
      - name: user_custom_id
        description: either null or the user_id from the gitlab_dotcom database
      - name: event_type
        tests:
          - not_null
      - name: page_view_id
        tests:
          - not_null
          - unique

  - name: manage_snowplow_smau_events
    description: '{{ doc("manage_snowplow_smau_events") }}'
    columns:
      - name: user_snowplow_domain_id
        description: UUID which is generated by the Javascript tracker and stored in a (first party) cookie
        tests:
          - not_null
      - name: user_custom_id
        description: either null or the user_id from the gitlab_dotcom database
      - name: event_type
        tests:
          - not_null
      - name: page_view_id
        tests:
          - not_null

  - name: plan_snowplow_smau_events
    description: '{{ doc("plan_snowplow_smau_events") }}'
    columns:
      - name: user_snowplow_domain_id
        description: UUID which is generated by the Javascript tracker and stored in a (first party) cookie
        tests:
          - not_null
      - name: user_custom_id
        description: either null or the user_id from the gitlab_dotcom database
      - name: event_type
        tests:
          - not_null
      - name: page_view_id
        tests:
          - not_null
          - unique

  - name: verify_snowplow_smau_events
    description: '{{ doc("verify_snowplow_smau_events") }}'
    columns:
      - name: user_snowplow_domain_id
        description: UUID which is generated by the Javascript tracker and stored in a (first party) cookie
        tests:
          - not_null
      - name: user_custom_id
        description: either null or the user_id from the gitlab_dotcom database
      - name: event_type
        tests:
          - not_null
      - name: page_view_id
        tests:
          - not_null
          - unique
