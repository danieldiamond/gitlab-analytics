version: 2

sources:
  - name: pings_tap_postgres
    database: raw
    schema: tap_postgres
    loaded_at_field: _uploaded_at

    quoting:
      database: false
      schema: false
      identifier: false

    tables:
      - name: usage_data
        identifier: version_db_usage_data
      - name: version_checks
        identifier: version_db_version_checks